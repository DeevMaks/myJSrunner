import Scene from '../Engine/Scene.js';

class Main extends Scene {
	constructor (actors, canvas) {
		super(actors, canvas);
	}

	calc (diffTime) {
		super.calc(diffTime);
	}


}

export default Main;
