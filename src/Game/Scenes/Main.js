import Scene from '../../Engine/Scene';

class Main extends Scene {
	constructor (canvas) {
		super(canvas);
	}

	calc (diffTime) {
		super.calc(diffTime);
		// console.log(this.container);
	}


}

export default Main;
